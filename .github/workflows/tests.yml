name: Run Tests

on:
  push:
    branches:
        - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run tests
        run: |
          echo "🧪 Running tests..."
          # Simulate tests
          echo "All tests passed ✅"