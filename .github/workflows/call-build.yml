name: call-build

on: 
    push:
        branches: 
            - master

jobs:
    resuable-build:
        uses: ./.github/workflows/resuable-build.yml
        with:
            app_name: "my=webu"
            environment: "production"
        secrets:
            docker_token: ${{ secrets.DOCKER_TOKEN }}

        