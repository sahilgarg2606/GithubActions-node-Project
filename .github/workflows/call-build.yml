name: call-build

on: 
    push:
        branches: 
            - master

jobs:
    call-build:
        uses: ./.github/workflows/reusable.yml
        with:
            app_name: "webu"
            environment: "production"

        