name: use composite

on: 
    push:
        branches:
            - master

jobs:
    tests:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4

            - name: run costom greet
              id: greet
              uses: ./.github/actions/setup-and-greet/actions.yml
              with:
                name: "Sahil"
                langauge: "Punjabi"

            - name: checkout output
              run: |
                echo "Greeted: ${{ steps.greet.outputs.greeted }}"